# Tasks

- Update `actions/upload-pages-artifact` to v5.0.0 in action.yml
- Fix shell injection in build_flags / check_flags
- Expose `include-hidden-files` input
- Support Git submodules (upstream: getzola/github-pages#2)
- Improve error message when `config.toml` is missing (upstream: getzola/github-pages#4)
- Add `base_url` override input
- Add CI tests for the action itself
- Configure Dependabot for GitHub Actions dependencies
- Expand README with more usage examples and a troubleshooting section
